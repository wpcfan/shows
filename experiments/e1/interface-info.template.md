# E1 真实接口信息表（填写后据此编写 adapter）

> **安全红线**：不要在本文件或聊天中写 API Key/密钥。adapter 通过环境变量读取（建议 `E1_API_KEY`）。
> 只需要「调用形状」：URL、字段名、鉴权头名称、脱敏后的请求/响应示例。

## 1. 接口与鉴权（写入 E1 `interface_name` / `interface_version`）

- 平台/接口名：
- 接口版本 + 来源（文档链接 / 接口返回字段 / 发布日期）：
- Base URL：
- 鉴权方式（header 名 + 前缀，示例：`Authorization: Bearer <E1_API_KEY>`）：
- 区域 / Endpoint / 是否需要代理：
- 从哪台机器或网络可访问（是否 IP 白名单）：

## 2. 模型

- provider / model id：
- 版本如何指定（请求字段？固定端点？）：
- 响应里能否读到模型版本（字段路径）：
- 是否支持 seed：字段名、取值范围、是否确定性（同 seed 同输出）：

## 3. 生成请求（A/B/C 三组的关键）

- HTTP method + path：
- 完整请求体示例（脱敏，务必体现以下字段）：
  - prompt 字段名：
  - **A 组「首帧参数位」字段名 + 取值示例**：
  - **B 组「参考图位」字段名 + 取值示例**（支持多图？顺序语义？）：
  - **C 组「无图」如何表达**（省略字段 / null / 空数组）：
  - duration 字段名 + 官方值域（最小/最大，来源与日期）：
  - ratio / resolution 字段与取值：
  - seed 字段：
- 图片传入方式（base64 / URL / multipart）与约束（格式、尺寸、大小、张数）：

## 4. 异步/同步与结果获取

- 提交响应示例（脱敏）：`task_id` / `request_id` 字段名：
- 查询接口（method + path）与状态枚举（进行中 / 成功 / 失败）：
- 成功响应中结果 URL 的字段路径；URL 有效期：
- 失败响应示例与错误码（限流 / 审核 / 超时 / 参数错）：
- 超时/重试语义：是否支持幂等键；重试是否重复计费：

## 5. 产物与规格

- 输出格式 / 编码 / 分辨率 / FPS / 时长是否可控：
- 是否有水印：
- 返回视频时长与请求 duration 是否一致：

## 6. 能力记录（E1 报告元数据，必须有来源/日期）

- 官方 lip-sync 支持声明 + 来源 + 验证日期：
- 最大可接受 padding（后期静帧延长上限的经验/文档值）：
- 计费单位、失败是否计费、并发/限流、单轮配额：

## 7. 最小集（只给这 6 项也能先写 adapter 跑单样本 smoke）

1. Base URL 与鉴权 header 名
2. 提交请求体示例（含 prompt / 首帧字段 / 参考图字段 / duration 字段）
3. 查询与结果字段路径
4. model id
5. duration 字段与官方值域
6. seed 字段

> 拿到最小集后：先写 adapter + A/B/C 各 1 次 smoke → 通过后再按分层配置跑校准与正式实验。
